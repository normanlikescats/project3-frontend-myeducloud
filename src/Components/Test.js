import React from "react";
import { useState, useEffect } from 'react';

export default function Test(){

  useEffect(()=>{
    //axios.get(`${process.env.REACT_APP_BACKEND_URL}/questionnaire/edit/${props.questionId}`)
  },[])

  return(
    <div>
      All the Tests
      
    </div>
  )
}